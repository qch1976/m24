# 忽略微信云开发调试目录
.cloudbase/

# 硬约束：项目根目录不允许 .mjs 文件（防 untracked dev-era 遗留）
# 依据：task-59 hotfix + AGENTS.md《文件放置惯例》
# 所有 .mjs 必须放 sub-folder（selftest/ tester/ tools/verify/ tools/），
# ESM 副本例外（js/core/*.mjs js/utils/*.mjs 等 sub-folder 内的 .mjs 不受影响）
/*.mjs

# tmp 临时 log/debug 文件
/_*.txt
/_*.log
/_*.tmp
/_*.mjs

# 根目录运行产物（不带下划线前缀的 selftest/bench 输出）
# 依据：task-64 经理裁定 2026-08-04 —— 原 /_*.log 要求下划线前缀，
#   导致 selftest_input05_*.log / sim-pack-after.log 漏网被 commit。
# ⚠️ 刻意用根目录锚定 + selftest/ 定向，禁用全局 *.log：
#   全局会误伤 sub-folder 下有意保留的日志（当前无，但规则要防未来）。
/*.log
/out-bench-*.json
tools/verify/out-bench-*.json

# selftest/ 目录运行产物（该目录下 .log 均为脚本输出，无有意保留项）
selftest/*.log

# task-80: tools/verify/gen-input06-recip-seeds.mjs 的运行产物（任意目录），不入库
input06-recip-seeds.json

# task-111: 截图验证临时装的 canvas 依赖（仅本地渲染 PNG 用，不入库）
node_modules/
